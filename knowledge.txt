Python 列表是一种有序集合，用方括号表示，元素从0开始编号。
可以用 append 方法往列表末尾添加元素，用 len 函数获取长度。
Python 字典用花括号表示，里面是键值对，用键来取值。
RAG 是检索增强生成，先把文档切块并向量化存入数据库。
用户提问时，系统先检索最相关的文本块，再交给大模型生成答案。
向量数据库 Chroma 可以本地运行，无需启动服务器。
学习编程最好边做边学，用 AI 当陪练能加快理解。
